---
title: 홈 서버 구축기 02 - 내부 SSH & 외부 SSH
date: 2025-06-10 19:33:00
categories: home-server
tags:
  - Home Server
  - Server
  - AMD CPU Server
---

# 내부 SSH 연결
이제, 실제 서버에서 터미널도 가능하지만, OpenSSH를 마지막에 설치 하였을 것이므로, 일반적인 홈 네트워크 기준, SSH 접속도 가능하다.

실제 서버 터미널에서 아래 명령어를 입력한다.
```shell
ip addr
```

서버의 네트워크 카드 환경에따라, enp11s0, wIp12s0 등의 IP를 확인한다.
필자의 경우 192.168.45.245/24이다.

해당 IP로 홈 네트워크에 공유기 연결된 (와이파이도 가능) PC에서 SSH 접속을 해주면 되며,
ID는 우분투 설치할때 만든 ID로 설정하면 된다.

# 외부 SSH 연결
하지만, 서버란 외부에서 사용할 수 있어야 진정한 서버 아니겠는가...
필자의 네트워크 기준으로 외부에서 서버 PC로 SSH 접속을 할 수 있도록 해보려고 한다.


## 기존 네트워크 구조 파악
- SKB (SK) 공유기 사용 중
  - 유동 IP
- 도메인 보유 중

일단 대략적인 개념을 알아야 한다. 

첫 번째로, 외부에서 나의 홈으로 트래픽이 오는 것이니 홈 공유기 설정부터 파악해보자.

일반적인 가정집 공유기 유동 IP 환경에서 https://ip.pe.kr/ 이런 사이트에서 나오는 내 IP가
외부에서 접속 가능한 IP이며 유동 IP이므로 주기적으로 바뀌는 IP이다.

필자는 1.241.82.207 IP로 나오며 해당 IP로 웹에 접속 시, 아래처럼 공유기 관리자 페이지가 나온다.

![](/images/HomeServer02-01.pngrver02-01.png)

공유기 비밀번호 변경 관련 내용은 생략한다. (변경하는 것을 추천)


참고로 보안상 아래처럼 내부 네트워크 기기로 내부 주소로 접근하는 방법을 추천한다.
본인의 공유기를 파악한 후, 내부 네트워크 기기에서 공유기 관리자 페이지로 접근한다 필자는 sk 공유기이다.
http://192.168.45.1/start.asp


즉, 공유기 관리자 페이지 로그인 및 관리는 내부에서(192.168...)으로 접근하고, 외부에서 오는 트래픽만 저 외부 IP를 통하게 만들 것이다.
일단, 저 1.241. IP는 유동적으로 변경되므로 DDNS를 사용해 볼 것이다.

freedns.afraid.org에 가입하여 진행한다.
![](/images/HomeServer02-02.pngrver02-02.png)

Add a subdomain을 클릭한다.

아래 이미지처럼 아까 나온 유동 IP를 입력한후 save한다.
![](/images/HomeServer02-03.pngrver02-03.png)

내부의 공유기 관리자 페이지로 접근한다.

freedns의 계정, 비번과 등록한 도메인 정보로 적용 시켜준다.

![](/images/HomeServer02-04.pngrver02-04.png)

아래 사이트에서 전파가 될 때 까지 기다린다.
https://dnschecker.org/
![](/images/HomeServer02-05.pngrver02-05.png)


전파가 끝나면, 해당 도메인으로 유동 IP에 접근 가능하며 공유기에 설정했기 때문에, 
도메인과 변경되는 유동IP가 자동으로 연결된다.
![](/images/HomeServer02-06.pngrver02-06.png)


이제 필자는 가지고 있는 가비아 도메인을 활용하여 도메인이 저 DDNS 주소를 바라보게 할 것이다.
일단 네임서버를 기본 가비아가 아닌 변경했다면 가비아로 돌려야한다.

그리고 아래와 같이 CNAME을 설정한다.
![](/images/HomeServer02-07.pngrver02-07.png)


아래와같이 포트포워딩을 추가한다.
![](/images/HomeServer02-08.pngrver02-08.png)


그 다음 외부에서 SSH Client Tool (putty 등)로
ssh.hyunjun.kr:8282에 연결하면 홈서버에 ssh로 연결이 될 것이다.

자 지금의 대략적인 구조는 아래와 같다.

ssh.hyunjun.kr:8282 --> hyunjun.mooo.com:8282 ---freedns---> 1.241.82.207 --> 공유기:8282>22로 포워딩 ---> 192.168.45.24:22(홈 서버)

이때 만약 :80 포트로 접근한다면 (브라우저에서 ssh.hyunjun.kr로)
ssh.hyunjun.kr(80) --> hyunjun.mooo.com(80) ---freedns---> 1.241.82.207(80) --> 공유기:80, 즉 관리자 페이지가 열림

즉, 위의 ssh.hyunjun.kr, hyunjun.mooo.com, 1.241.82.207 모두 내 홈의 관리자 페이지가 열린다.

이걸 skb 공유기 관리자 페이지에서 막을 수 있지만 막으려면 ddns를 비활성화 해야한다고 나온다.


해서 아래처럼 편법으로 관리자 페이지를 막는다.

가비아에서 루트 도메인 hyunjun.kr을 cname으로 추가적으로 설정해준다.
![](/images/HomeServer02-09.pngrver02-09.png)


그리고 공유 관리자 페이지에서 아래처럼 80을 포트포워딩 해주면,
![](/images/HomeServer02-10.pngrver02-10.png)



이제, 아까 나열했던 3가지 주소들로 접근해도 관리자 페이지가 열리지 않는다. (80포트를 홈서버의 80에 연결한 것)
![](/images/HomeServer02-11.pngrver02-11.png)

해서 공유기 관리자 페이지는 공유기 내부 네트워크 안에서 http://192.168.45.1/ 로만 접근한다.
이렇게 내부에서 접근할 때는 포트 포워딩 규칙에 걸리지 않는다.



